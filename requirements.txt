# Servers / UI
fastapi==0.99.1
uvicorn[standard]==0.30.1
streamlit==1.36.0
requests==2.32.3

# Microsoft / SharePoint
msal==1.28.0
Office365-REST-Python-Client==2.5.12

# OpenAI SDK (used directly in your code)
openai==1.40.0

# Retrieval (legacy, Pydantic v1 era)
chromadb==0.4.24
pydantic==1.10.13
langchain==0.1.20
langchain-community==0.0.36
# (no langchain-openai needed; your code calls openai SDK directly)

# Embeddings (only if Python 3.11; remove on 3.13 to avoid torch wheels)
sentence-transformers==2.2.2

# Docs / PDF (you said no PPTX)
python-docx==1.1.2
docx2txt==0.8
pypdf==4.2.0
pdfminer.six==20231228

# Misc
tiktoken==0.7.0
# don't pin lxml; python-docx will pull a compatible version
