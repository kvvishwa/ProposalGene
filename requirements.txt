# --- APIs / servers (keep if you use these modes) ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
gradio==4.41.0

# --- App UI ---
streamlit==1.36.0
requests==2.32.3

# --- Microsoft auth / SharePoint client ---
msal==1.28.0
Office365-REST-Python-Client==2.5.12

# --- OpenAI SDK (new client) ---
openai==1.40.0

# --- Retrieval stack (pins aligned to fix chroma.config issue) ---
chromadb==0.5.3
langchain==0.2.10
langchain-core==0.2.35
langchain-community==0.2.10
langchain-openai==0.1.6   # (ok with LC 0.2.x; bump only if you actually use it)

# --- Embeddings / Transformers (CPU-only, stable) ---
sentence-transformers==2.6.1

transformers==4.41.2
huggingface-hub==0.23.4

# --- Vector alt (optional). Comment out if you don't really use FAISS paths ---
# faiss-cpu==1.7.4

# --- Document parsing ---
python-docx==1.1.2
docx2txt==0.8
python-pptx==0.6.23
Pillow==10.3.0

# Prefer PyMuPDF (your code uses it) over heavy PDF stacks
PyMuPDF==1.24.2
# pypdf==4.2.0             # optional; not needed if you rely on PyMuPDF
# pdfminer.six==20231228    # optional; not needed if you rely on PyMuPDF

# --- Data / utils ---
pandas==2.2.2
numpy==1.26.4
tiktoken==0.7.0
lxml==4.9.4

# --- SQLite shim (AVOID unless your App Service image has sqlite < 3.35) ---
# pysqlite3-binary==0.5.2.post3 ; platform_system == "Linux"
