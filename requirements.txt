# --- APIs / servers (optional) ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
gradio==4.41.0

# --- App UI ---
streamlit==1.36.0
requests==2.32.3

# --- Microsoft / SharePoint ---
msal==1.28.0
Office365-REST-Python-Client==2.5.12

# --- OpenAI SDK ---
openai==1.40.0

# --- Retrieval stack (aligned on 0.2 line) ---
chromadb==0.5.3
langchain==0.2.10
langchain-community==0.2.10
langchain-openai==0.2.14     # << key: matches the 0.2 core line
# intentionally DO NOT pin langchain-core

# --- Embeddings / NLP ---
sentence-transformers==2.6.1  # pulls a compatible torch on Py 3.11
# (If you're on Python 3.13, consider removing sentence-transformers to avoid torch wheels.)



# --- Embeddings / Transformers (CPU) ---
sentence-transformers==2.6.1
# (Do NOT pin torch here; it will be pulled automatically for py3.11)

# --- Document parsing ---
python-docx==1.1.2
docx2txt==0.8
PyMuPDF==1.24.2

# --- Data / utils ---
pandas>=2.2.2
numpy>=1.26.4
tiktoken>=0.7.0
lxml>=4.9.3

# --- SQLite shim (only if your App Service sqlite < 3.35; otherwise omit) ---
# pysqlite3-binary==0.5.2.post3 ; platform_system == "Linux"
